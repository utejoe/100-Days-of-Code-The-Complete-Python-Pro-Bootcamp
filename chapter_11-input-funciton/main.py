# This line of code prints a greeting message that includes the user's name.
# It first prompts the user to enter their name using the input function,
# then concatenates the greeting "Hello " with the user's input, and finally prints the result.

print("Hello " + input("what is your name: \n"))
