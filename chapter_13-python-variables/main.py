# Prompt the user to input their name and store it in the variable 'name'
name = input("What is your name? \n")

# Calculate the length of the input string (the user's name) using the len() function
length = len(name)

# Print the length of the input string (the number of characters in the user's name)
print(length)
